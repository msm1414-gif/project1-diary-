# 日記Bot 設計指示書（次チャットへの引き継ぎ）

このドキュメントは、設計相談チャットの結果をまとめた引き継ぎ資料です。
**まだ実装は始めていません。設計フェーズの途中です。** 次のセッションはこれを起点に進めてください。

---

## 1. プロダクトコンセプト

LINEで動く日記Bot。コアの体験は次の一文：

> **「日記を書く」のではなく「Botと雑談したら、気づいたら日記になっていた」**

- 夜、Botのほうから雑談をしかけてくる。6〜7往復ゆるく話すと一日が言語化される。
- 日中ふと思いついたことを送ってもいい。
- Botは前の発言・日中のメッセージ・先週の流れを覚えていて反応する。
- 当面は **作者本人のみが使う個人利用**。

機械っぽさを減らすことを最重要視する：
- 前の回答に反応してから次を聞く（フォームではなく会話）
- 言い回しは会話文脈からAIが毎回生成 → 自動的にバリエーションが出る
- 昨日・先週・カレンダーの予定を覚えていて触れる（継続性）
- 毎日全項目を聞かない。揃った項目はスキップ、流れで深掘り
- Botの人格（口調・距離感）を一つ決めて一貫させる

---

## 2. 決定済み事項

| 項目 | 決定 | 補足 |
|---|---|---|
| 言語/基盤 | Node.js + TypeScript / **Vercel** | webhook=サーバーレス関数、定時処理=Vercel Cron |
| AI | **Gemini API**（無料枠重視） | 要約処理は抽象化してClaudeに差し替え可能にする。※Claude Pro MaxはAPIアクセスを含まない点に注意 |
| DB | **Supabase Postgres** | SQLiteはVercelサーバーレスと相性が悪いため不採用。カレンダーとプロバイダを揃えやすい |
| 会話のタイミング | **夜にまとめて1セッション** | Botが会話を開始し6〜7往復 |
| 日中メッセージ | **いつでも受け付ける／静かに保存** | Botは即レスせず、夜のセッションでまとめて触れる |
| 要約サイクル | **週次** | 当初「月次」→「週次ループ型」に変更 |
| 要約の成果物 | **日ごと日記＋週次振り返りの両方** | 文章として読めるレベル。文学性は不要 |
| カレンダー連携 | **読み取り専用で連携** | 方式は要確認（後述）|

LINEの制約：無料プランはpush月200通まで、reply（会話中の返信）は無制限。
Botが夜に口火を切る1通だけpush＝月約30通で枠内に収まる。

---

## 3. 週次ループ（処理フロー）

```
日中   : ユーザーの自発メッセージを保存（Botは静かに受け取る）
夜     : Botが会話セッションを開始（push 1通）
         日中の発言＋週次プラン＋未充足スロットを見て自然に質問
         6〜7往復で一日の情報が揃う → その日の「生ログ」をDBに保存
週初め : AI週次プランニング
         入力 = 先週の週次まとめ ＋ 今週のカレンダー予定
         出力 = 「週次質問プラン」（日付ごとの重点テーマ＋予定アンカー）
         例: 水=プレゼン本番→夜は本番の振り返り重点 / 金=友人と食事→人の話を聞く
週末   : AIが1週間の生ログを清書 → 日ごと日記 ＋ 週次まとめ
         週次まとめが次週プランニングの入力になる → ループが閉じる
```

重要な原則：
- **週次プランは台本ではなくガイド**。当日予定が変わったり日中に別の話が出たら、AIはプランより流れを優先する。
- **カレンダー予定＝質問の燃料**。予定のタイトル・参加者・時間帯がそのまま具体的な問いになる（「今日いちばん印象に残ったことは?」より「今日のプレゼンどうだった?」）。

---

## 4. 日記スロット設計

調査ベースの10項目（出来事・感情の記録／感情ラベリング／感謝・ポジティブな出来事／
目標設定と進捗記録／タスク・ToDoリスト／学習内容の要約・振り返り／反省・改善点／
進捗・成果の可視化／体調・調子の記録／創造的アイデア）を、重複を統合して **8スロット** にした。

| スロット | 元項目 | 聞き方イメージ |
|---|---|---|
| **A 出来事＋感情** | 出来事＋感情ラベリング | 「今日いちばん〇〇だったことは?」→「どんな気持ちだった?」→感情ラベル付与 |
| **B 感謝・よかったこと** | 感謝ジャーナル | 「今日ちょっと良かったこと、ひとつ挙げると?」 |
| **C 体調・調子** | 体調記録 | 軽い選択式（◎/○/△）＋一言 |
| D 目標・進捗・成果 | 目標進捗＋成果可視化 | 「例の〇〇、今日は進んだ?」 |
| E タスク・ToDo | タスクリスト | 「明日やっておきたいことある?」 |
| F 学習・気づき | 学習の振り返り | 「今日知って『へぇ』と思ったこと、あった?」 |
| G 反省・改善点 | 反省・改善 | 「今日やり直せるなら変えたいこと、ある?」 |
| H 創造的アイデア | アイデアメモ | 「ふと思いついたこと、メモしておきたいの ある?」 |

方針：
- 10項目を毎晩6〜7往復で全部聞くのは不可能。**毎晩のコア＋ローテーション**で運用する。
- **A・B・C を「毎晩のコア」候補**とする（調査でも日々の継続が効くとされる3つ）。
- **D〜H は「ローテーション」**。毎晩そのうち1〜2個だけ聞く。聞く内容が毎日変わり機械っぽさも減る。

データ設計への効き目：
- 感情ラベルは決まった語彙（喜び/怒り/不安/悲しみ/安心/疲れ…）で持つ → 週次振り返りで傾向が出せる
- 体調は数値（3段階）で持つ → 天候・曜日との相関が見える
- 各スロットを構造化フィールドで持てば、週次清書で「感謝リスト」「目標の進捗まとめ」がきれいに作れる

---

## 5. カレンダーアプリ（project0）の現状

- リポジトリ：`msm1414-gif/project0`（**別リポジトリ**）。デプロイ先 `project0-three.vercel.app`。
- 名称「Timebox Calendar」。Next.js + TypeScript + Tailwind CSS。
- **永続化は IndexedDB（ブラウザ内）。サーバー側DBは無い。** ログイン不要。
- 既存機能に連携の手がかり：
  - **ICS購読機能**（スマホのカレンダーアプリから予定を閲覧）
  - 共有トークン手動入力 ＋ QRコード対応
  - 朝8時のプッシュ通知（＝Vercel Cronでサーバー処理が動いている）
- `lib/` 構成：`types.ts`（Event/Todo/Category）, `db.ts`（IndexedDB, idb）,
  `store.ts`（Zustand）, `time.ts`, `colors.ts`, `categorize.ts`

---

## 6. 連携の課題と方針（最重要・未解決）

**課題**：カレンダーの予定は IndexedDB ＝ ユーザーのブラウザ内にしか無い。
LINE Botはサーバーで動くため、そのデータに直接は触れられない。
**リポジトリを統合してもこのデータ問題は解決しない**（アーキテクチャの問題でリポジトリ構成の問題ではない）。

**希望の光**：ICS購読／共有トークン／プッシュ通知が成立している以上、
共有トークン単位で予定データが何らかの形でサーバー側に同期されている可能性が高い。
もしそうなら、**既存のICSフィードを日記Botがfetchして解析するのが最もきれいな連携路**
（新APIすら不要かもしれない）。ICSは標準フォーマットで解析が容易。

**確定のために必要な作業**：project0のコード、特に
ICS購読・共有トークン・プッシュ通知の実装（`app/api/` 配下が核心）を読み、
予定データが本当にサーバー側にあるのか・トークン単位でどう保存しているのかを確認する。

**進め方の合意**：project0のコードをこのリポジトリに持ち込みモノレポ化する。
理由は (1) Claudeのアクセスが `project1-diary-` に限定されており、同リポジトリにあれば
カレンダーのコードを直接読めるようになる、(2) Event/Todo型やDBクライアントを共有でき
デプロイも一体化できる。提案構成：

```
project1-diary-/
  apps/
    calendar/     ← project0 の中身
    diary-bot/    ← これから作る日記Bot
  packages/
    shared/       ← Event/Todo 型などの共有コード
```

注意：**稼働中の project0 のVercelデプロイを壊さないこと**。
Vercel設定を切り替えるまで project0-three.vercel.app は生かしておく。

---

## 7. 未決定事項（次チャットで決める）

1. **ローテーション項目（D〜H）の出し方**：AIが会話の流れで選ぶ／曜日固定／毎晩全部軽く聞く
2. **毎晩必ず聞くコアの確定**：A・B・C のみ／＋G反省／＋Eタスク
3. **カレンダー連携の具体方式**：既存ICSフィード再利用／共有トークンの保存先を共有／新API追加
   （project0のコードを読んでから確定）
4. データモデル（テーブル設計）の詳細
5. Botの人格・口調の具体化
6. 各スロットの質問文の具体的な言い回し

---

## 8. 想定データモデル（たたき台）

- `users` — LINEユーザーID、登録日、配信時刻設定、Bot口調設定
- `messages` — 生メッセージログ（送信者、本文、日時）。言い回し生成と継続性の燃料
- `daily_logs` — 日付ごとの生ログ、スロット充足状態
- `diary_entries` — 日付ごとの清書日記（週次バッチで生成）、元データも保持
- `weekly_summaries` — 週次まとめ。次週プランニングの入力
- `weekly_plans` — 週次質問プラン（日付×重点テーマ×予定アンカー）
- カレンダー予定はproject0側から連携取得（このDBには持たない or スナップショットのみ）

---

## 9. 次にやること（推奨順）

1. project0のコードをこのリポジトリに移し、モノレポ構成にする（稼働中デプロイは保護）
2. ICS購読・共有トークン・プッシュ通知の実装を読み、予定データがサーバー側にあるか確認
3. カレンダー連携方式を確定（セクション7-3）
4. 未決定のスロット運用（7-1, 7-2）を決める
5. データモデルを確定し、Supabaseのスキーマを作る
6. MVP実装に着手（LINE webhook → 会話 → 生ログ保存 を最小で通す）

---

## 環境メモ

- 作業ブランチ：`claude/daily-diary-ai-summary-GGY5k`
- ClaudeのGitHubアクセスは `msm1414-gif/project1-diary-` に限定（project0は直接読めない）
- 実行環境は一時的。成果物はコミット＆プッシュしないと次セッションに残らない
